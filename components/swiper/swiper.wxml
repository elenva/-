<block>
  <swiper 
      class='swiper'
      indicator-color="#ccc" 
      indicator-active-color="#fff">
    <swiper-item wx:for="{{swiperArray}}" wx:key="{{index}}">
      <image src="{{item}}" bindtap="swiperItemTap" data-current="{{item}}"></image>
    </swiper-item>
  </swiper>
</block>