<!--components/scoreBar/scoreBar.wxml-->
<wxs src="../../utils/format.wxs" module="format" />
<view class='wrap' bindtap="showInfo">
  当前积分余额：{{score}}
  <text wx:if="{{showCash}}">，可提现金额{{format.numberTofix(cash)}}元</text>
</view>
