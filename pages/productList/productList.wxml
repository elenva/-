<!--pages/productList/productList.wxml-->
<block>
  <sort bind:sort="sort"/>
</block>
<block>
  <view class='list'>
    <view class='listItem' wx:for="{{list}}" wx:key="{{index}}" bindtap="pro">
      <view class='left'>
        <image src="{{item?item.imgUrl:'/images/index/ad1.png'}}"></image>
      </view>
      <view class='right'>
        <view class='title'>
          {{item.name}}
        </view>
        <view class='status'>
          <mediaType media="{{item.type}}" class="media"/>
          <text>{{
            item.isfree === 1
            ?"已解锁"
            :"未解锁"
          }}</text>
        </view>
        <view class='info'>
          <text>{{item.courseView}}人浏览</text>
          <text>共{{item.keyword1}}节课 共{{item.keyword2}}分钟</text>
        </view>
      </view>
    </view>
  </view>
</block>
